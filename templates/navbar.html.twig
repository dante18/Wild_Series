<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">
        <i class="fa fa-ticket-alt pr-2"></i>Wild Series
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link active" href="{{ path('app_index') }}">Accueil</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="nav-item nav-link" href="{{ path('program_index') }}">Séries-admin</a>
                <a class="nav-item nav-link" href="{{ path('season_index') }}">Saisons-admin</a>
                <a class="nav-item nav-link" href="{{ path('episode_index') }}">Episodes-admin</a>
                <a class="nav-item nav-link" href="{{ path('category_index') }}">Catégories-admin</a>
                <a class="nav-item nav-link" href="{{ path('actor_index') }}">Acteurs-admin</a>
            {% else %}
                <a class="nav-item nav-link" href="{{ path('wild_index') }}">Séries</a>
            {% endif %}
            {% if app.user %}
                <a href="{{ path('app_profile') }}" class="nav-item nav-link"><h6>Mon compte</h6></a>
                <a href="{{ path('app_logout') }}" class="nav-item nav-link">
                    <h6>Se déconnecter</h6>
                </a>
            {% else %}
                <a href="{{ path('app_register') }}" class="nav-item nav-link">
                    <h6>S'inscrire</h6>
                </a>
                <a href="{{ path('app_login') }}" class="nav-item nav-link">
                    <h6>Se connecter</h6>
                </a>
            {% endif %}
        </div>
    </div>
</nav>